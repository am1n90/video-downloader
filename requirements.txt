# Пара строго согласована: yt-dlp и yt-dlp-ejs обновлять ТОЛЬКО вместе,
# версию ejs брать из METADATA нового yt-dlp (Requires-Dist: yt-dlp-ejs==X).
# curl_cffi тоже обновлять ТОЛЬКО вместе с yt-dlp: версию брать из METADATA
# нового yt-dlp (extra curl-cffi — допустимый диапазон, extra
# pin-curl-cffi — рекомендованная точная версия).
yt-dlp==2026.8.19
yt-dlp-ejs==0.8.0
curl_cffi==0.16.0
# Торрент-движок (торрент-стриминг, решение владельца №2: исключение из
# «только stdlib» как curl_cffi). Колесо есть только для cp313 — сборка на
# Python 3.13. Обновлять осознанно: прототип Этапа 0 проверен на 2.1.1.
libtorrent==2.1.1
Pillow>=10.0.0
PySide6>=6.6.0
PySide6-Fluent-Widgets>=1.5.0
